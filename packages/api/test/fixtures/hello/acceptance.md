# hello: acceptance

The black-box oracle the run is judged by. `test-design` locks it before
`build`, beside the verification manifest whose one required check is what
`verify` runs.

- running the check exits zero
